
import {mountLayout,badge,paginate,renderPagination,toast,openModal} from './common.js';import {documents} from './data.js';await mountLayout('document');let page=1;const per=6;function filtered(){const q=document.querySelector('#keyword').value.toLowerCase(),t=document.querySelector('#type').value,p=document.querySelector('#process').value,a=document.querySelector('#analysis').value;return documents.filter(d=>(!q||(d.notice+d.name).toLowerCase().includes(q))&&(!t||d.type===t)&&(!p||d.process===p)&&(!a||d.analysis===a))}function render(){const arr=filtered(),rows=paginate(arr,page,per);document.querySelector('#count').textContent=`총 ${arr.length}건`;document.querySelector('#tbody').innerHTML=rows.map(d=>`<tr><td>${d.id}</td><td>${d.notice}</td><td class="title-cell">${d.name}</td><td>${d.type}</td><td>${d.size}</td><td>${d.date}</td><td>${badge(d.process)}</td><td>${badge(d.analysis)}</td><td><button class="icon-btn" data-view="${d.id}">⋯</button></td></tr>`).join('')||`<tr><td colspan="9" class="empty">검색 결과가 없습니다.</td></tr>`;renderPagination(document.querySelector('#pagination'),arr.length,page,p=>{page=p;render()},per);document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{const d=documents.find(x=>x.id==b.dataset.view);const m=openModal('문서 상세',`<p><b>${d.name}</b></p><p>연결 공고: ${d.notice}</p><p>문서 유형: ${d.type}</p><p>처리 상태: ${d.process}</p><p>분석 상태: ${d.analysis}</p>`,`<button class="btn btn-outline" data-close>닫기</button><button class="btn btn-primary" id="retryBtn">재처리</button>`);m.querySelector('#retryBtn').onclick=()=>{toast('문서 재처리를 요청했습니다.');m.remove()}})}document.querySelector('#searchBtn').onclick=()=>{page=1;render()};document.querySelector('#refreshBtn').onclick=()=>toast('최신 처리 상태를 불러왔습니다.');render();
