# BGE-M3 임베딩 파이프라인 의존성
#
# PyTorch는 AWS GPU의 CUDA 환경에 맞춰 별도로 설치하는 것을 권장합니다.
# 설치 후 반드시 다음 명령으로 CUDA 인식 여부를 확인하세요.
# python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
#
# 현재 프로젝트 가상환경 예시:
# /home/ubuntu/ddokbot/venvs/one-cycle/bin/python

FlagEmbedding==1.4.0
numpy
transformers==4.57.1
accelerate
safetensors
tqdm
sentencepiece